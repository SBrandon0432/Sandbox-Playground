import React, { StrictMode } from 'react';
import ReactDOM from 'react-dom';
import './indexS.scss';

ReactDOM.render(
  <StrictMode>
    <div className="App">
        IM working!
    </div>
  </StrictMode>,

  document.getElementById('root')
);
